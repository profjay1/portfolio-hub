# Runbooks

One document per operational procedure, written so that someone (including future you at 2 a.m.) can follow it without context.
Planned: deploy, rollback, rotate a secret, restore from backup, rebuild the server, respond to an alert, renew or debug certificates.
Each runbook records the last date it was rehearsed.
