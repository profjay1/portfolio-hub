version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    groups:
      actions:
        patterns: ["*"]
  # Enable each block below in the milestone that creates the manifest (an entry pointing at a
  # missing directory produces Dependabot errors). M1 adds backend, frontend, and Docker.
  #
  # - package-ecosystem: "maven"
  #   directory: "/backend"
  #   schedule: { interval: "weekly" }
  #   commit-message: { prefix: "build(backend)" }
  #   groups:
  #     spring: { patterns: ["org.springframework*"] }
  # - package-ecosystem: "npm"
  #   directory: "/frontend"
  #   schedule: { interval: "weekly" }
  #   commit-message: { prefix: "build(frontend)" }
  # - package-ecosystem: "docker"
  #   directory: "/backend"
  #   schedule: { interval: "weekly" }
  # - package-ecosystem: "terraform"
  #   directory: "/infra/terraform"
  #   schedule: { interval: "weekly" }
